/**
 * collect the individual helper methods, so they are accessible via a single object
 */

import login from './common/login';
import elements from './common/elements';

export default {
  login,
  elements,
};
